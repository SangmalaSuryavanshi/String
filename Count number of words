class Solution 
{ 
    int countWords(String s) 
    { 
        // code here  
        s = s.replace("\\n", " ");
        s = s.replace("\\t", " ");
        s=s.replaceAll("[ ]+"," ");
        return s.trim().split(" ").length;
    }
} 
